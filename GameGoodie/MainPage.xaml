<!--We bind the KeyDown event to the Page Object-->
<!-- UIElement.PreviewKeyDown Event-->
<!--Occurs when a key is pressed while focus is on this element. Means: Page element)-->
    <Page
    x:Class="GameGoodie.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameGoodie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" PreviewKeyDown="Page_PreviewKeyDown"> 
    <!---end of page description-->

    <!--Bottom AppBar-->
    <!--Outside the grid so the grid doen't affect the Command bar-->
    <Page.BottomAppBar>
        <CommandBar IsSticky="True" IsOpen="True">
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Start" Icon="GoToStart" Click="NewGameButton_Click"/>
                <AppBarButton Label="Pause" Icon="Pause" Click="PauseGameButton_Click"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <!--It's time for grid-->
    <Grid>
        <!-- Canvas Element will hold Goodie, and Baddies as it's Children elements-->
        <Canvas Name="CanvasPlayingArea" Background="White"/>
    </Grid>
</Page>
